"use strict";

const sharp = require('sharp');
const Promise = require('bluebird');

class Resizer {
    constructor(opts) {
        this._opts = opts;
    }

    resize() {
        return new Promise((resolve, reject) => {
            //
        });
    }
}